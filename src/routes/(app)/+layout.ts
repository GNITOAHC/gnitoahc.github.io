export const prerender = true;

// Emit `foo/index.html` instead of `foo.html` so GitHub Pages redirects
// /notes/x to /notes/x/ instead of 404ing on the trailing slash.
export const trailingSlash = 'always';
